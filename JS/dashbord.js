// Simple interaction example: Display an alert when clicking a card
document.querySelectorAll('.card').forEach(card => {
    card.addEventListener('click', () => {
        alert('Fonctionnalité en cours de développement.');
    });
});
